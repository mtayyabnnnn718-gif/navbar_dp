# navbar_dp
A navigation bar component in Java-based applications that provides quick access to different sections of the system. It also includes a display picture (DP) area for showing the logged-in user’s profile image.
